const App = () => (
    <h1>
        Hello From react
    </h1>
)
export default App;
// what we did above is created a component called App which returns a h1 div.
//Then we exported this component so that we can use it in index.js
