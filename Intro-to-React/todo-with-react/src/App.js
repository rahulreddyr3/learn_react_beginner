import Button from "./components/Button";

const App = () => (
    <div>
        <h1>
            Hello From react
        </h1>
        <Button/>
    </div>
    // Note that we created an encompassing Div as we can only return one element in a component.
)
export default App;
